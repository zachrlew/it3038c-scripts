Make sure your system has the most current version of python in order to run this script.
Python download available at:  https://www.python.org/
Run using windows machine (Can open inside of Visual Studio Code and run from there, but
as long as python is up to date it should run fine by saving the script as a ".py" file to 
the system.)
